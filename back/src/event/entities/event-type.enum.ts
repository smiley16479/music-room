export enum EventType {
  LISTENING_SESSION = 'listening_session', // Playlist personnelle / écoute solo
  COLLABORATIVE = 'collaborative',          // Playlist collaborative
  PARTY = 'party',                         // Événement avec date/lieu
  LIVE_SESSION = 'live_session',           // Écoute en direct synchronisée
}
